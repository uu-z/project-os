interface:
  display_name: "Project OS"
  short_description: "Document-first project operating system"
  default_prompt: "Use $project-os in autopilot mode to turn this project into a document-first operating system, derive the active goal from KERNEL, strategy, project, blockers, and machine truth, repair the 0-90 control plane first, then drive the final 90-100 execution loop."
